"use strict";

// функціі рекурсивна
function sayHi() {
  var name = prompt("enter your name");
  if (name === null) return;
  name = neme.trim();
  console.log(Name);

  if (name !== "") {
    alert("Hello, ".concat(name, "!"));
  } else {
    sayHi;
  }
}

sayHi();